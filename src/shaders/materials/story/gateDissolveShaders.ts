import fragmentShader from './gate-dissolve.frag.glsl?raw';
import vertexShader from './gate-dissolve.vert.glsl?raw';

export const gateDissolveShaders = {
  fragmentShader,
  vertexShader,
} as const;
