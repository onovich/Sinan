# Asset Pipeline Reports

This directory is reserved for isolated AssetPipelineAdapter spike reports and tiny generated evidence.

Source fixtures remain under `spikes/mature-dependencies/fixtures/**` and are the durable truth. Generated GLB or JSON outputs under this reports directory are rebuildable evidence only and must not be treated as source assets.
